{
  "name": "commands-agent-cli",
  "version": "0.0.1",
  "type": "module",
  "description": "CLI companion for commands.agent",
  "bin": {
    "commands-agent": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "check": "bun --check src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^26.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
